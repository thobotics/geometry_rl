defaults:
  - transform: default

name: Isaac-Ant-v0
seed: 0
num_envs: 4000
device: cuda
warmup_steps: 0
eval_type: "mode" # "random" or "mode"
extra:

env_info:
  root_key: log
  prefix_key_name: Reward

# Video recording settings
video: False
video_length: 0
video_interval: 2000
video_episode_trigger: 0
video_dir: output_videos